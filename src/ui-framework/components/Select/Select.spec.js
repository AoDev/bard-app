// import React from 'react'
// import {shallow} from 'enzyme'
// import {expect} from 'chai'
// import sinon from 'sinon'

// import Selector from './Selector'

describe('<Select />', () => {
  test.todo('should display all options from an array of strings')
  test.todo('should display all options from an array of objects')
  test.todo('should detect if the value is a number and parse it on emit')

  describe('the optional item', () => {
    test.todo('should be displayed by using ')
  })

  describe('when using arrays of strings', () => {
    test.todo('should use the string both as label and value')

    describe('onChange event', () => {
      test.todo('should emit the string')
    })
  })

  describe('when using arrays of objects', () => {
    test.todo('should use the property given by objectLabelKey for the label')
    test.todo('should use the property given by objectValueKey for the value')

    describe('onChange event', () => {
      test.todo('should be able to emit the whole object')
      test.todo('should be able to emit the objet key')
      test.todo('should be able to emit the DOM event')
      test.todo('should be able to emit the select input name with the value')
    })
  })
})
